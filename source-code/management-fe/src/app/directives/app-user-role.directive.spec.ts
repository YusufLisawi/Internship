import { AppUserRoleDirective } from './app-user-role.directive';

describe('AppUserRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new AppUserRoleDirective();
    expect(directive).toBeTruthy();
  });
});
